# Não defina a api_key aqui em produção — configure-a nas Environment Variables do projeto na Vercel.
# Este arquivo serve só para rodar "vercel dev" localmente. Copie para ".env" e preencha.

ALLOWPAY_API_KEY=allow_apikey_ekls4xtcgap
ALLOWPAY_BASE_URL=https://allow-gi0i.onrender.com

# Opcional (recomendado): segredo para assinar/validar o webhook.
ALLOWPAY_WEBHOOK_SECRET=troque_por_um_segredo_forte

# Opcional: URL pública HTTPS do deploy, para ativar o webhook (ex: https://seusite.vercel.app).
PUBLIC_URL=
